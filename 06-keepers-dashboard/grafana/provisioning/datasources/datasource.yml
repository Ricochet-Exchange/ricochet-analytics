# config file version
apiVersion: 1

deleteDatasources:
  - name: Prometheus
    orgId: 1

# list of datasources to insert/update 
datasources:
- name: loki
  type: loki
  orgId: 1
  url: http://10.2.1.55:3100
  password:
  user:
  database:
  basicAuth: false
  basicAuthUser:
  basicAuthPassword:
  withCredentials:
  jsonData:
     graphiteVersion: "1.1"
     tlsAuth: false
     tlsAuthWithCACert: false
  secureJsonData:
    tlsCACert: "..."
    tlsClientCert: "..."
    tlsClientKey: "..."
  version: 1
  editable: true 
  url: localhost

- name: Prometheus
  type: prometheus
  access: proxy
  orgId: 1
  url: http://10.2.1.55:9090
  password:
  user:
  database:
  basicAuth: false
  basicAuthUser:
  basicAuthPassword:
  withCredentials:
  isDefault: true
  jsonData:
     graphiteVersion: "1.1"
     tlsAuth: false
     tlsAuthWithCACert: false
  secureJsonData:
    tlsCACert: "..."
    tlsClientCert: "..."
    tlsClientKey: "..."
  version: 1
  editable: true
